import { Card, CardHeader, CardTitle } from "@/components/ui/card";

export default function BankingResult() {
    return (
        <Card className="w-[600px]">
            <CardHeader>
                <CardTitle>Kết quả chuyển tiền</CardTitle>
            </CardHeader>
        </Card>
    )
}